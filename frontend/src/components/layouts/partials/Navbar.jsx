export default function Navbar() {
    return (
        <header
            className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300 py-4 bg-white/95 backdrop-blur-sm`}
        >
            Đây là Navbar

        </header>
    )
}